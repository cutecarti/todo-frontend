# PostgreSQL
POSTGRES_USER=todo
POSTGRES_PASSWORD=todo
POSTGRES_DB=todo
POSTGRES_PORT=5432
POSTGRES_HOST=localhost
POSTGRES_DOCKER_HOST=db

# Prisma (локально — localhost, в Docker app подставляется из compose)
DATABASE_URL=postgresql://todo:todo@localhost:5432/todo?schema=public

# API base URL для клиента (пусто = тот же origin, /api/...)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
